#pragma once

// ============================================================
// 应用全局配置
// ============================================================

// 设备信息
#define APP_NAME                    "Clawd Mochi"
#define APP_VERSION                 "1.0.0"
#define APP_AUTHOR                  "Clawd"

// 调试模式
#define APP_DEBUG                   true

// 主循环间隔 (ms)
#define APP_LOOP_INTERVAL_MS        10

// 状态机更新间隔 (ms)
#define STATE_MACHINE_INTERVAL_MS   50

// 显示刷新间隔 (ms)
#define DISPLAY_REFRESH_INTERVAL_MS 33  // ~30fps
